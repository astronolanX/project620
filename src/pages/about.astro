---
import Layout from '../layouts/Layout.astro';
import BottomDrawer from '../components/BottomDrawer.astro';
---

<Layout title="About - Nolan">
  <main class="about-container">
    <header class="about-header">
      <h1>About</h1>
    </header>

    <div class="content-wrapper">
      <section class="intro-section">
        <div class="intro-text">
          <h2>Hey there, I'm Nolan</h2>
          <p>I'm a creative technologist obsessed with the intersection of design, code, and human experience. Currently building interfaces that don't suck and occasionally writing about it.</p>
          
          <p>My work spans across web development, creative coding, and digital product design. I believe in crafting experiences that are not just functional, but delightful and accessible to everyone.</p>
        </div>
        
        <div class="intro-image">
          <img src="https://picsum.photos/400/500?random=10" alt="Portrait" />
        </div>
      </section>

      <section class="image-break">
        <img src="https://picsum.photos/800/300?random=11" alt="Workspace aesthetic" />
        <p class="image-caption">My current setup - where the magic happens</p>
      </section>

      <section class="skills-section">
        <h2>What I Do</h2>
        <div class="skills-grid">
          <div class="skill-card">
            <h3>Frontend Development</h3>
            <p>React, Vue, Svelte, and whatever comes next. I love building interfaces that feel smooth and respond beautifully to user interaction.</p>
            <div class="skill-level">
              <div class="skill-bar" style="--width: 90%;"></div>
            </div>
          </div>

          <div class="skill-card">
            <h3>Creative Coding</h3>
            <p>Generative art, WebGL experiments, and interactive installations. Code as a creative medium fascinates me endlessly.</p>
            <div class="skill-level">
              <div class="skill-bar" style="--width: 75%;"></div>
            </div>
          </div>

          <div class="skill-card">
            <h3>Design Systems</h3>
            <p>Building scalable, maintainable design systems that help teams move fast without breaking things.</p>
            <div class="skill-level">
              <div class="skill-bar" style="--width: 85%;"></div>
            </div>
          </div>

          <div class="skill-card">
            <h3>Performance</h3>
            <p>Obsessed with making the web faster. Core Web Vitals, bundle optimization, and everything in between.</p>
            <div class="skill-level">
              <div class="skill-bar" style="--width: 80%;"></div>
            </div>
          </div>
        </div>
      </section>

      <section class="image-break">
        <img src="https://picsum.photos/600/400?random=12" alt="Code on multiple screens" />
        <p class="image-caption">Late night coding sessions and too much coffee</p>
      </section>

      <section class="philosophy-section">
        <h2>My Philosophy</h2>
        <div class="philosophy-content">
          <div class="philosophy-text">
            <p>I believe technology should enhance human capability, not replace human judgment. Every line of code I write is an opportunity to make someone's day a little bit better.</p>
            
            <p>Accessibility isn't an afterthought—it's fundamental to good design. If your interface isn't usable by everyone, it's not done yet.</p>
            
            <p>Performance is a feature. Fast websites don't just rank better; they respect users' time and data.</p>
          </div>
          <div class="philosophy-image">
            <img src="https://picsum.photos/350/300?random=13" alt="Minimalist workspace" />
          </div>
        </div>
      </section>

      <section class="currently-section">
        <h2>Currently</h2>
        <div class="currently-grid">
          <div class="currently-item">
            <span class="currently-label">Reading</span>
            <p>"The Design of Everyday Things" by Don Norman (for the 4th time)</p>
          </div>
          
          <div class="currently-item">
            <span class="currently-label">Learning</span>
            <p>Rust for WebAssembly and exploring the boundaries of web performance</p>
          </div>
          
          <div class="currently-item">
            <span class="currently-label">Building</span>
            <p>A generative art platform that makes creative coding accessible to everyone</p>
          </div>
          
          <div class="currently-item">
            <span class="currently-label">Listening</span>
            <p>Mostly lo-fi beats and the occasional podcast about the future of the web</p>
          </div>
        </div>
      </section>

      <section class="final-image">
        <img src="https://picsum.photos/800/400?random=14" alt="Beautiful sunset coding" />
        <p class="image-caption">Sometimes the best ideas come at golden hour</p>
      </section>
    </div>
  </main>
  
  <BottomDrawer />
</Layout>

<style>
  .about-container {
    min-height: 100vh;
    background: white;
    padding: clamp(2rem, 5vw, 4rem);
    max-width: 900px;
    margin: 0 auto;
  }

  .about-header {
    text-align: center;
    margin-bottom: clamp(3rem, 8vw, 6rem);
  }

  .about-header h1 {
    font-size: clamp(3rem, 8vw, 6rem);
    font-weight: 800;
    margin-bottom: 1rem;
    color: black;
    text-shadow: 
      -0.1em 0 #ff3333,
      0.1em 0 #33ccff,
      -0.15em 0 rgba(255, 51, 51, 0.8),
      0.15em 0 rgba(51, 204, 255, 0.8);
  }

  .content-wrapper {
    display: flex;
    flex-direction: column;
    gap: clamp(4rem, 8vw, 6rem);
  }

  .intro-section {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: start;
  }

  .intro-text h2 {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: black;
  }

  .intro-text p {
    font-size: clamp(1.1rem, 2.5vw, 1.3rem);
    line-height: 1.7;
    color: #444;
    margin-bottom: 1.5rem;
  }

  .intro-image img {
    width: 100%;
    height: 400px;
    object-fit: cover;
    border-radius: 12px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
  }

  .image-break {
    text-align: center;
    margin: 2rem 0;
  }

  .image-break img {
    width: 100%;
    max-height: 400px;
    object-fit: cover;
    border-radius: 12px;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  }

  .image-caption {
    font-size: 0.9rem;
    color: #666;
    font-style: italic;
    margin-top: 1rem;
  }

  .skills-section h2 {
    font-size: clamp(1.8rem, 4vw, 2.2rem);
    font-weight: 700;
    margin-bottom: 2rem;
    text-align: center;
    color: black;
  }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .skill-card {
    padding: 2rem;
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: 12px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .skill-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .skill-card h3 {
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: black;
  }

  .skill-card p {
    color: #666;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .skill-level {
    width: 100%;
    height: 6px;
    background: rgba(0, 0, 0, 0.1);
    border-radius: 3px;
    overflow: hidden;
  }

  .skill-bar {
    height: 100%;
    background: linear-gradient(90deg, #ff3333, #33ccff);
    width: var(--width);
    border-radius: 3px;
    transition: width 1s ease;
  }

  .philosophy-section h2 {
    font-size: clamp(1.8rem, 4vw, 2.2rem);
    font-weight: 700;
    margin-bottom: 2rem;
    color: black;
  }

  .philosophy-content {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: clamp(2rem, 5vw, 3rem);
    align-items: center;
  }

  .philosophy-text p {
    font-size: clamp(1.1rem, 2.5vw, 1.2rem);
    line-height: 1.7;
    color: #444;
    margin-bottom: 1.5rem;
  }

  .philosophy-image img {
    width: 100%;
    height: 250px;
    object-fit: cover;
    border-radius: 12px;
  }

  .currently-section h2 {
    font-size: clamp(1.8rem, 4vw, 2.2rem);
    font-weight: 700;
    margin-bottom: 2rem;
    color: black;
  }

  .currently-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
  }

  .currently-item {
    padding: 1.5rem;
    background: rgba(0, 0, 0, 0.02);
    border-radius: 8px;
    border-left: 4px solid #ff3333;
  }

  .currently-label {
    font-weight: 600;
    color: #333;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .currently-item p {
    margin: 0.5rem 0 0 0;
    color: #666;
    line-height: 1.5;
  }

  .final-image {
    text-align: center;
  }

  .final-image img {
    width: 100%;
    max-height: 300px;
    object-fit: cover;
    border-radius: 12px;
  }

  @media (max-width: 768px) {
    .intro-section,
    .philosophy-content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .intro-image img {
      height: 300px;
    }

    .skills-grid {
      grid-template-columns: 1fr;
    }

    .currently-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 480px) {
    .about-container {
      padding: 2rem 1rem;
    }
  }
</style>