---
import Layout from '../layouts/Layout.astro';
import StickyFooter from '../components/StickyFooter.astro';
---

<Layout title="Nolan Figueroa - Design & Technology">
  <div class="page-wrapper">
    <!-- Home Header -->
    <header class="home-header">
      <div class="intro-badge">Available for new projects</div>
      <h1 class="home-title">Design & Creative Technology</h1>
      <p class="home-subtitle">Bridging design and tech to transform how people and organizations come together through innovative experiences</p>
    </header>

    <!-- Bento Grid Layout -->
    <main class="bento-container">
      <!-- Large introduction card -->
      <div class="bento-item large">
        <div class="bento-content">
          <h2>About</h2>
          <p>I'm a designer and technologist focused on creating meaningful experiences at the intersection of creativity and innovation.</p>
          <p>Currently exploring the future of human-computer interaction through spatial computing, AI-enhanced design tools, and accessible interface patterns.</p>
          <div class="experience-years">
            <span class="years-number">8+</span>
            <span class="years-label">Years Experience</span>
          </div>
        </div>
      </div>

      <!-- Medium capability cards -->
      <div class="bento-item medium">
        <div class="bento-content">
          <div class="capability-icon">⚡</div>
          <h3>Design Systems</h3>
          <p>Scalable foundations for digital products that serve millions of users.</p>
          <div class="capability-metric">
            <span class="metric-value">15+</span>
            <span class="metric-label">Systems Built</span>
          </div>
        </div>
      </div>

      <div class="bento-item medium">
        <div class="bento-content">
          <div class="capability-icon">🔬</div>
          <h3>Research & Strategy</h3>
          <p>User-centered approaches to solving complex organizational challenges.</p>
          <div class="capability-metric">
            <span class="metric-value">120+</span>
            <span class="metric-label">Studies Conducted</span>
          </div>
        </div>
      </div>

      <!-- Small highlight cards -->
      <div class="bento-item small">
        <div class="bento-content">
          <h4>Current Focus</h4>
          <div class="focus-list">
            <span class="focus-item">AI Design Tools</span>
            <span class="focus-item">Spatial Computing</span>
            <span class="focus-item">Accessibility</span>
          </div>
        </div>
      </div>

      <div class="bento-item small">
        <div class="bento-content">
          <h4>Location</h4>
          <div class="location-info">
            <span class="location-primary">San Francisco Bay Area</span>
            <span class="location-timezone">PST/PDT</span>
          </div>
        </div>
      </div>

      <div class="bento-item small">
        <div class="bento-content">
          <h4>Work Style</h4>
          <div class="work-style">
            <span class="style-type">Remote-First</span>
            <span class="style-detail">Global collaboration</span>
          </div>
        </div>
      </div>

      <div class="bento-item small">
        <div class="bento-content">
          <h4>Tools</h4>
          <div class="tools-list">
            <span class="tool-item">Figma</span>
            <span class="tool-item">React</span>
            <span class="tool-item">Framer</span>
          </div>
        </div>
      </div>

      <!-- Medium capability cards continued -->
      <div class="bento-item medium">
        <div class="bento-content">
          <div class="capability-icon">🛠️</div>
          <h3>Creative Technology</h3>
          <p>Prototyping and experimental interfaces that push boundaries.</p>
          <div class="capability-metric">
            <span class="metric-value">50+</span>
            <span class="metric-label">Prototypes</span>
          </div>
        </div>
      </div>

      <div class="bento-item medium">
        <div class="bento-content">
          <div class="capability-icon">🎯</div>
          <h3>Product Strategy</h3>
          <p>From concept to market-ready solutions with measurable impact.</p>
          <div class="capability-metric">
            <span class="metric-value">25+</span>
            <span class="metric-label">Product Launches</span>
          </div>
        </div>
      </div>

      <!-- Wide testimonial/philosophy card -->
      <div class="bento-item wide">
        <div class="bento-content">
          <h3>Design Philosophy</h3>
          <div class="philosophy-grid">
            <div class="philosophy-item">
              <span class="philosophy-number">01</span>
              <p>Human-centered design isn't just about users—it's about understanding the full ecosystem of stakeholders</p>
            </div>
            <div class="philosophy-item">
              <span class="philosophy-number">02</span>
              <p>Technology should amplify human capability, not replace human judgment</p>
            </div>
            <div class="philosophy-item">
              <span class="philosophy-number">03</span>
              <p>The best interfaces are invisible—they fade away to let people focus on their goals</p>
            </div>
          </div>
        </div>
      </div>
    </main>

    <StickyFooter />
  </div>
</Layout>

<style>
  .page-wrapper {
    min-height: 100vh;
    background: linear-gradient(135deg, var(--color-home-lighter) 0%, var(--color-home-light) 100%);
    padding-left: 100px; /* Space for floating menu */
  }

  .home-header {
    padding: 3rem 2rem 2rem;
    text-align: center;
  }

  .intro-badge {
    display: inline-block;
    background: var(--color-home);
    color: white;
    font-family: 'Roboto Mono', monospace;
    font-size: 0.875rem;
    font-weight: 600;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .home-title {
    font-family: 'Roboto', sans-serif;
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 900;
    line-height: 1.1;
    margin: 0 0 1rem;
    color: var(--color-home);
  }

  .home-subtitle {
    font-size: 1.25rem;
    line-height: 1.5;
    color: #333;
    max-width: 700px;
    margin: 0 auto;
  }

  .bento-container {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    grid-template-rows: repeat(6, minmax(200px, auto));
    gap: 1.5rem;
    padding: 2rem;
    max-width: 1400px;
    margin: 0 auto;
  }

  /* Bento grid layouts */
  .bento-item.large {
    grid-column: span 8;
    grid-row: span 2;
  }

  .bento-item.medium {
    grid-column: span 4;
    grid-row: span 2;
  }

  .bento-item.small {
    grid-column: span 3;
    grid-row: span 1;
  }

  .bento-item.wide {
    grid-column: span 12;
    grid-row: span 2;
  }

  .bento-item {
    background: rgba(255, 255, 255, 0.9);
    border-radius: 20px;
    padding: 2rem;
    backdrop-filter: blur(10px);
    border: 2px solid rgba(255, 255, 255, 0.2);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .bento-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--color-home);
    border-radius: 20px 20px 0 0;
  }

  .bento-item:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(0, 119, 255, 0.2);
    border-color: var(--color-home-light);
  }

  .bento-content h2 {
    font-family: 'Roboto', sans-serif;
    font-size: 2rem;
    font-weight: 700;
    margin: 0 0 1rem;
    color: var(--color-home);
  }

  .bento-content h3 {
    font-family: 'Roboto', sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 0 1rem;
    color: var(--color-home);
  }

  .bento-content h4 {
    font-family: 'Roboto', sans-serif;
    font-size: 1.125rem;
    font-weight: 700;
    margin: 0 0 0.75rem;
    color: var(--color-home);
  }

  .bento-content p {
    color: #666;
    line-height: 1.6;
    margin: 0 0 1rem;
  }

  /* Experience years styling */
  .experience-years {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: 2rem;
    padding-top: 2rem;
    border-top: 2px solid var(--color-home-lighter);
  }

  .years-number {
    font-family: 'Roboto Mono', monospace;
    font-size: 3rem;
    font-weight: 900;
    color: var(--color-home);
    line-height: 1;
  }

  .years-label {
    font-size: 0.875rem;
    font-weight: 600;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* Capability icons */
  .capability-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    display: block;
  }

  /* Metrics styling */
  .capability-metric {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 2px solid var(--color-home-lighter);
  }

  .metric-value {
    font-family: 'Roboto Mono', monospace;
    font-size: 2rem;
    font-weight: 900;
    color: var(--color-home);
    line-height: 1;
  }

  .metric-label {
    font-size: 0.875rem;
    font-weight: 600;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* Focus items */
  .focus-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .focus-item {
    background: var(--color-home-lighter);
    color: var(--color-home);
    font-size: 0.875rem;
    font-weight: 600;
    padding: 0.5rem 1rem;
    border-radius: 12px;
    border: 1px solid var(--color-home-light);
    text-align: center;
  }

  /* Location info */
  .location-info {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .location-primary {
    font-weight: 600;
    color: var(--color-home);
  }

  .location-timezone {
    font-family: 'Roboto Mono', monospace;
    font-size: 0.875rem;
    color: #666;
  }

  /* Work style */
  .work-style {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .style-type {
    font-weight: 600;
    color: var(--color-home);
  }

  .style-detail {
    font-size: 0.875rem;
    color: #666;
  }

  /* Tools */
  .tools-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .tool-item {
    background: var(--color-home-lighter);
    color: var(--color-home);
    font-size: 0.75rem;
    font-weight: 600;
    padding: 0.25rem 0.75rem;
    border-radius: 12px;
    border: 1px solid var(--color-home-light);
    text-align: center;
  }

  /* Philosophy */
  .philosophy-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .philosophy-item {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .philosophy-number {
    font-family: 'Roboto Mono', monospace;
    font-size: 1.5rem;
    font-weight: 900;
    color: var(--color-home);
    flex-shrink: 0;
  }

  .philosophy-item p {
    margin: 0;
    font-size: 0.95rem;
    line-height: 1.5;
    font-style: italic;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .page-wrapper {
      padding-left: 0;
    }

    .bento-container {
      grid-template-columns: 1fr;
      padding: 1rem;
      gap: 1rem;
    }

    .bento-item.large,
    .bento-item.medium,
    .bento-item.small,
    .bento-item.wide {
      grid-column: span 1;
      grid-row: span 1;
    }

    .bento-item {
      padding: 1.5rem;
    }

    .home-header {
      padding: 2rem 1rem 1rem;
    }

    .philosophy-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .focus-list {
      flex-direction: row;
      flex-wrap: wrap;
    }

    .tools-list {
      flex-direction: row;
      flex-wrap: wrap;
    }
  }
</style>